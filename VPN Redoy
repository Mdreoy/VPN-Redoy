<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VPN Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .vpn-status { font-weight: bold; }
        .connected { color: green; }
        .disconnected { color: red; }
        .connection-card { transition: all 0.3s; }
        .connection-card:hover { transform: translateY(-5px); box-shadow: 0 10px 20px rgba(0,0,0,0.1); }
    </style>
</head>
<body>
    <div class="container py-5">
        <h1 class="text-center mb-5">VPN Management Portal</h1>
        
        <!-- Status Dashboard -->
        <div class="row mb-4">
            <div class="col-md-6">
                <div class="card connection-card">
                    <div class="card-body">
                        <h5 class="card-title">VPN Connection</h5>
                        <p>Status: <span id="vpnStatus" class="vpn-status disconnected">Disconnected</span></p>
                        <button id="connectBtn" class="btn btn-success me-2">Connect</button>
                        <button id="disconnectBtn" class="btn btn-danger" disabled>Disconnect</button>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card connection-card">
                    <div class="card-body">
                        <h5 class="card-title">Server Status</h5>
                        <p>Active Connections: <span id="activeConnections">0</span></p>
                        <p>Data Transferred: <span id="dataTransferred">0 MB</span></p>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Configuration Section -->
        <div class="card mb-4">
            <div class="card-header">
                <h5>VPN Configuration</h5>
            </div>
            <div class="card-body">
                <form id="configForm">
                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label for="serverLocation" class="form-label">Server Location</label>
                            <select class="form-select" id="serverLocation">
                                <option value="us-east">US East</option>
                                <option value="us-west">US West</option>
                                <option value="europe">Europe</option>
                                <option value="asia">Asia</option>
                            </select>
                        </div>
                        <div class="col-md-6">
                            <label for="protocol" class="form-label">Protocol</label>
                            <select class="form-select" id="protocol">
                                <option value="udp">UDP (Faster)</option>
                                <option value="tcp">TCP (More reliable)</option>
                            </select>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label for="username" class="form-label">Username</label>
                        <input type="text" class="form-control" id="username" required>
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label">Password</label>
                        <input type="password" class="form-control" id="password" required>
                    </div>
                    <button type="submit" class="btn btn-primary">Generate Configuration</button>
                </form>
            </div>
        </div>
        
        <!-- Connections Table -->
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="mb-0">Active Connections</h5>
                <button id="refreshBtn" class="btn btn-sm btn-outline-secondary">Refresh</button>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>User</th>
                                <th>IP Address</th>
                                <th>Connected Since</th>
                                <th>Duration</th>
                                <th>Data Up/Down</th>
                            </tr>
                        </thead>
                        <tbody id="connectionsTable">
                            <!-- Will be populated by JavaScript -->
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="vpn-manager.js"></script>
</body>
</html>
